use bevy::prelude::*;

pub struct MachineListPlugin;

impl Plugin for MachineListPlugin {
    fn build(&self, _app: &mut App) {
        // app.add_systems(Startup, Self::load_ui)
        //     .add_systems(Update, Self::draw_ui);
    }
}
